version: 3.8

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - chroma_data:/app/chroma_db
      - scraped_data:/app/data/scraped
volumes:
  chroma_data:
  scraped_data: